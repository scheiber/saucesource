{
  "name": "back-end",
  "version": "1.0.0",
  "engines": {
    "node": "18.7.0"
  },
  "description": "hot sauce catalog api server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:init": "psql -U postgres -f db/schema.sql",
    "db:seed": "psql -U postgres -f db/seed.sql",
    "start": "node server.js"
  },
  "author": "Jonathan Scheiber",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.8.0",
    "pg-promise": "^12.7.0"
  }
}
